version: 1

network:
  domains:
    app-domain: ~ 

  routes:
    web-route:
      domain: app-domain
      path: /
      # Имя сервиса должно совпадать с именем в docker-compose.yml
      service: frontend
      port: 80
      type: http