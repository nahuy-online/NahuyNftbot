
# NFT Lottery Mini App

Приложение для продажи NFT и проведения лотереи в Telegram.

## 🔗 Конфигурация и URL

### 1. Локальный запуск
Для работы локально создайте файл `.env` в корне проекта (используйте `.env.example` как основу):
```bash
cp .env.example .env
```
Убедитесь, что `VITE_API_TARGET` указывает на ваш запущенный бэкенд (обычно `http://localhost:8080`).

### 2. Деплой (Production)
Приложение настроено как "Монолит" (Бэкенд раздает Фронтенд).
*   **VITE_API_URL**: Указывать **не обязательно**. Приложение автоматически будет использовать относительный путь `/api`, что идеально подходит для деплоя на Render, Heroku или VPS.
*   **В BotFather**: Когда вы задеплоите приложение (например, на Render), вы получите URL вида `https://my-nft-app.onrender.com`. Именно этот URL нужно указать в настройках вашего бота в Telegram (BotFather -> My Bots -> Bot Settings -> Menu Button -> Configure Menu Button).

## ⚠️ Важное замечание о запуске

Если при установке вы видите сообщение:
`found 7 vulnerabilities (4 moderate, 1 high, 2 critical)`
**Не пугайтесь.** Это стандартное сообщение аудита зависимостей. Оно **не мешает** запуску приложения в режиме разработки. Просто продолжайте выполнение команд.

## Как запустить (Самый простой способ - Docker)

Самый надежный способ запустить и фронтенд, и бэкенд, и базу данных одной командой:

1. Убедитесь, что установлен [Docker Desktop](https://www.docker.com/products/docker-desktop/).
2. В файле `docker-compose.yml` вставьте ваш `BOT_TOKEN`.
3. Запустите:
   ```bash
   docker-compose up --build
   ```
4. Откройте `http://localhost:80` (Это симуляция веб-приложения).

## Запуск вручную (Без Docker)

Если вы хотите запустить только интерфейс (Frontend) для проверки верстки:

1. Установите зависимости:
   ```bash
   npm install
   ```
2. Запустите:
   ```bash
   npm run dev
   ```
3. Приложение откроется на `http://localhost:3000`.
   *Примечание: Если бэкенд не запущен, включите "Demo Mode" на экране ошибки.*

## Переменные окружения Бэкенда
Для продакшна (например, в Render Dashboard) укажите:
- `BOT_TOKEN`: Токен вашего Telegram бота.
- `ADMIN_IDS`: ID админов через запятую (например `12345,67890`).
- `RECEIVER_ADDRESS`: Ваш кошелек TON для приема платежей.
- `DATABASE_URL`: (Автоматически выдается Render PostgreSQL).
